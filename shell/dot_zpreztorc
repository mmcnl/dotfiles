# -*- mode: shell-script -*-

# Set the Prezto modules to load (browse modules).
# The order matters.
zstyle ':prezto:load' pmodule \
       'environment' \
       'terminal' \
       'history' \
       'completion' \
       'syntax-highlighting'
