# -*- mode: shell-script -*-

# set private env vars and config in file shared between bash and zsh
source "$HOME/.sh_env"

# export bash specific env vars
export SHELL_BASENAME=zsh

# on OSX, all shells are started as login shells so it's important to source
# .bashrc now to set up for an interactive session
source "$HOME/.bashrc"
